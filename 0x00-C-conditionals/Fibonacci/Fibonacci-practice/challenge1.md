🔹 Challenge 1 — Fibonacci with Validation & Clean Output
🧩 Problem Upgrade

Write pseudocode that:

1️⃣ Input Validation
Prompts user for n

If n <= 0, print:

Invalid input

and stop

2️⃣ Clean Output Format

Output must look like:

Fibonacci sequence:
0 1 1 2 3 5 8

👉 Important:

Numbers should be on one line
Proper spacing (no messy output)
3️⃣ Core Logic
Same Fibonacci logic you already built
Must handle:
n = 1
n = 2
Use:
loop
state variables (first, second, etc.)
⚠️ New Thinking Focus

This time I am watching for:

✔ Input validation flow

Do you stop correctly when input is invalid?

✔ Output discipline

Where do you print:

"Fibonacci sequence:"

Before? After? Inside loop?

✔ Clean structure

No unnecessary duplication like:

printing logic scattered everywhere
messy edge-case handling
✍️ YOUR TASK

Write pseudocode that includes:

Input validation (n <= 0)
Clean header output
Correct Fibonacci logic
Clean formatting

💡 Tip (not a hint, just guidance):
Think of your program in 3 blocks:

Input + validation
Output header
Sequence generation
